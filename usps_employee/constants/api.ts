// This Mac's address on the local network, where the Spring Boot backend runs.
// "localhost" would mean the phone itself, so a physical device needs the LAN
// IP. Update this if the network hands out a different address.
export const API_BASE = "http://10.0.0.54:8080";
